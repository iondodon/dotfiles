new_tab main
layout horizontal
title scheduler-app
cd ~
launch bash
cd ~/vpn
launch --bias=-15 bash

new_tab scheduler-app
layout horizontal
title scheduler-app
cd ~/scheduler-app
launch bash
launch --bias=-15 bash
